export interface MetricCard {
  label: string;
  value: string | number;
  icon: string;
}

export interface WorkItem {
  title: string;
  subtitle: string;
  status: string;
}

export interface QuickAction {
  label: string;
  icon: string;
}
